import React from "react";
import ReactDOM from "react-dom/client";
import SchemaVisualizer from "./App";
import "./main.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SchemaVisualizer />
  </React.StrictMode>
);
